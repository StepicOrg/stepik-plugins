#include <tunables/global>

{{ sandbox_java_bin }} {
    #include <abstractions/base>

    {{ sandbox_jdk_home }}/**         rix,
    {{ sandbox_clojure_jar }}         r,
    /etc/passwd                       r,
    {{ arena_dir }}**                 r,
}
